node_modules/
dist/
.next/
*.tsbuildinfo

.env
.env.local
.env.*.local

.DS_Store
*.log

# Office lock files, created while a document in docs/ is open
~$*

# Vercel
.vercel

# Env templates are committed on purpose -- the README tells you to copy them.
# Do not broaden the .env rules above to `.env*`, which swallows these.
!.env.example
!**/*.env.local.example
